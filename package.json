{
  "name": "wrtconf",
  "version": "0.0.1",
  "description": "Simple WebRTC conference coordinator",
  "main": "index.js",
  "repository": "https://github.com/PalokhaO/wrtconf",
  "author": "Oleksandr Palokha <palokha.oa@gmail.com>",
  "license": "MIT",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "cd packages/server && yarn build && cd ../client && yarn build",
    "start": "cd packages/test-project && yarn start",
    "publish": "cd packages/server && yarn publish && cd ../client && yarn publish && cd ../models && yarn publish"
  }
}
